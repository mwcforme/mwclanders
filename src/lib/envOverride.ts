/**
 * @deprecated Stage environment removed 2026-05-25.
 * Stubs retained so any lingering import doesn't break the build.
 * Safe to delete this file once all callers are removed.
 */
export function hasEnvOverride(): false { return false; }
export function setEnvOverride(_next: string): void { /* no-op */ }
